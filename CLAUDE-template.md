# CLAUDE-template.md

> ## 如何使用本模板——完善后删除本节，并将文件改名为 `CLAUDE.md`
>
> 这是一份**通用骨架**，不是要一次性填满的表格。据此完善某个具体项目时：
>
> 1. **能从代码/配置读出来的内容，自己读代码填**：项目概述、架构说明、运行与部署、配置与凭证约束这类章节，
>    读一遍代码、README、配置示例文件基本就能写准确。
> 2. **"核心设计原则"和"关键设计决策"这两节不要自己脑补，必须向用户确认**。这两节记录的是真实踩过的坑
>    和权衡过的取舍，不是读代码能猜出来的"看似合理"的规则——凭代码推测出几条填进去，读起来像模像样，
>    但很可能是编的，会误导后续开发。没有把握的内容，先问用户，或者先留空/留 TODO，不要为了"填满骨架"
>    而编造。
> 3. **允许并且应该大幅精简**：一个几百行的小脚本项目不需要 14 个章节，参照现有代码骨架里
>    "只需要几行规范"的最简形式——只保留文档规范和代码风格里的硬约束即可，不必强行套用完整结构。
> 4. **这是随项目生长的文档，不是一次性产出**：初期可以只写"项目概述 + 通用约束"，等真正做出取舍、
>    踩过坑之后再回来补"关键设计决策"；"实现状态"章节应随开发进度持续更新，不要一开始就假装写全。
>    更新已有章节时优先用新结论覆盖/替换旧的，不要为每次变化单独追加一段记录，避免文档变成流水账。
> 5. **删除本节和所有 `[方括号占位符]`，并把文件名从 `CLAUDE-template.md` 改为 `CLAUDE.md`**，
>    模板才算完成对某个项目的定制——`CLAUDE-template.md` 这个文件名本身就是"尚未完善"的信号，
>    完善过程中和完善后都不要让它以这个名字留在项目里生效。
> 6. **"架构说明"章节要写成可供快速定位的索引，不是事后补的流水账**：每个模块条目尽量给出
>    具体的函数/类名或"什么需求会涉及这里"的关键词，而不是一句笼统的职责描述。这样后续处理
>    需求时能先查这份索引锁定 1-3 个相关文件，不必因为不确定而通读大半个项目，这正是"通用开发
>    约束"里定位习惯要解决的问题。

本文档为 Claude Code 提供指导，帮助在开发 **[项目名称/一句话定位，如：xxx 同步工具 / xxx 监控服务]** 时理解项目上下文、设计原则与开发约束。

无论用户输入包含哪种语言，包括英文代码、报错信息或专业术语，都用简体中文回答和解释；仅当用户明确要求"用英语回答"或"翻译"时才使用其他语言。

## 需求处理流程（强制）

除非用户明确说明是"顺手""小改""直接改"这类不需要确认的琐碎调整，否则收到任何功能性需求时，**必须先完成理解与确认，再动手实现**：

1. **复述需求**：用自己的话简要复述理解到的需求，包含隐含的前提与边界：涉及范围、是否要兼容旧数据、是否要动已有行为。如果需求本身有歧义或多种合理理解，明确指出分歧点，不要自行选一种理解就往下做。
2. **给出简要计划**：列出打算改动的文件/模块、大致思路，以及会不会涉及本文件"核心设计原则"或"关键设计决策"里的约束。计划保持简短，几行到十几行即可，不是完整设计文档。
3. **等待用户确认后才动手实现**，包括写代码、跑测试、提交代码。用户可能会在这一步纠正理解偏差或调整方向，此时按新的理解重新给出复述和计划，而不是直接开始改。

该流程的意义是尽早发现"需求理解错了"或"方向错了"，避免做完一大圈工作后才发现要推倒重来；能省一步确认的简单任务不必生搬硬套。

> 多份文档各司其职时请在此说明分工与冲突时的优先级，例如：
> [README.md](README.md) 面向使用者，讲功能特性、配置与使用方法；本文件面向开发，讲关键设计决策与操作性约束。
> 若还有独立的设计文档，也应在此列出并说明与本文件冲突时以谁为准。
> 两者/三者冲突时以 README.md 为准，并同步修订本文件。
> 只有一份文档时删除本段。

## 项目概述

[用 3-6 行说明：这是什么类型的项目：Web 服务、CLI 脚本集、采集同步工具、桌面工具或其他，解决什么问题，
核心工作流程是什么。可以用一段话 + 几条要点列出主要能力，每条要点标注对应的入口文件，格式：
`- **XX 功能**：一句话说明该模块做什么，见 [xxx.py](xxx.py)。`]

当前处于**[编码前期 / 持续迭代 / 已归档等阶段说明，可选，视项目状态删减]**。

## 核心设计原则（最高优先级）

[列出决定这个项目"正确性"的少数几条约束——通常是那些一旦违反就会导致数据错误、
安全问题或不可恢复后果的规则，而不是普通的代码风格偏好。每条给出理由和涉及的关键文件/函数，
标注 **【MUST】**。数量控制在 3-7 条，太多说明还没抓住重点。示例格式：]

1. **【MUST】[原则名称]** —— [一句话说明规则内容]。[说明为什么，以及具体实现位置，如 `xxx.py` 的 `func_name()`]。

2. **【MUST】[原则名称]** —— ...

## 通用开发约束

- **【MUST】** 保持代码简洁易懂，避免过度设计和不必要的抽象；不擅自添加用户未要求的功能。
- **【MUST】** 用户提出需求时，先阅读 [README.md](README.md) 和相关模块代码，理解当前项目目标、架构与实现方式。
- **【MUST】** 功能/架构发生变化后同步更新 README.md 与本文件，避免文档与代码脱节。
- **【MUST】** 新增功能时创建新模块或函数，不要把逻辑全部塞进已有的大文件或大函数。
- **【MUST】** 项目特性按用户需求分阶段实现，不需要一次性把 README/设计文档里列出的所有特性都做完。
- **【MUST】** 处理需求前先查"架构说明"定位到相关模块，用 grep/关键字搜索确认涉及范围，
  只读取真正相关的文件；不要在不确定范围时就通读整个项目或大量无关文件，避免不必要的上下文消耗。
  "架构说明"和"关键设计决策"没有覆盖到、确实无法判断影响范围时，再扩大搜索或直接询问用户，
  而不是靠通读源码来补足文档的缺口。
- **【SHOULD】** 查询外部资源时优先参考官方文档，专业论坛为辅，中文互联网资源仅供参考。

## 代码风格

- **【MUST】** 不在代码注释、日志输出、控制台输出中使用 emoji 表情及特殊符号，终端显示可能异常；README.md 与文档中可以使用。
- **【MUST】** 密码、密钥、token、API secret 等敏感信息严禁写入日志、异常信息，或提交到仓库。
- **【MUST】** 网络请求、外部 API 调用、文件读写等操作须捕获异常并记录日志，失败要可见，不静默吞掉；单点失败不中断整体流程，做到故障隔离。
- **【MUST】** 日志使用统一的 logger 实例，不用 `print`，不在业务模块里另起日志初始化。
- **【SHOULD】** 使用类型注解提升可读性，Python 用 `from __future__ import annotations` 加类型提示，TypeScript 同理。
- **【SHOULD】** 函数和类添加简短的 docstring/注释说明用途与参数，注释密度与同文件其它同类条目保持一致。
- **【SHOULD】** 新增第三方依赖前先评估必要性，保持依赖精简，同步更新依赖声明文件。

[以下为该项目特有的代码风格补充：]

- [项目特定风格 1]
- [项目特定风格 2]

[项目为 Windows 相关工具、需在中文系统运行时，保留以下编码与换行符约束：]

- **【MUST】** 明确各文件类型的编码要求：`.py`/`.md` 用 UTF-8，`.bat` 在中文系统上可能需要 GB2312 而非 UTF-8，`.ps1` 在 Windows PowerShell 5.1 下需要 UTF-8 with BOM 才能正确显示中文；在 `.vscode/settings.json` 与 `.gitattributes` 中固化这些要求，避免编辑器保存时改回默认编码。
- **【MUST】** 明确换行符要求：`.bat` 需要 CRLF，其余文件用 LF；同样在 `.gitattributes` 中固化。
- **【MUST】** 编码特殊的文件无法用常规文本编辑工具直接修改时，说明改用什么方式修改。

## 文档规范

README.md、CLAUDE.md 及代码注释遵守：

- 只客观描述功能、现状、使用方法与约束，不强调"做了哪些修改/优化"、不解释"为什么这么改"；代码注释、commit message、对话回复同理。确需长期保留原因的，写进"关键设计决策"这类明确标注为决策记录的章节。
- 篇幅精简，不写长篇大论：一句话能说清楚的不写第二句，不铺垫背景、不重复自证；docstring/注释长度应与同一文件里其它同类函数/条目保持一致的密度。
- **篇幅纪律**：每次改完本文件检查总行数，持续变长就做减法，不要继续加内容。建议为 CLAUDE.md 设一个明确的行数上限并写在这里；超出上限时压缩已有条目，不要放宽上限。
- **不记录可直接查询的现状**：机器上一条命令就能拿到的内容不写进文档。需要留痕的，写"用什么命令查"而不是"查出来的值"。
- **只写现状，不写沿革**：不写"X 原为 Y""曾判断为""本次""第 N 次调整"这类叙述；对象一旦删除或替换，就不必再被提及。
- **保留结论，不保留过程**：只写当前结论与取舍逻辑，不写推导和排查过程；同类信息只在一处展开，别处一行指回。新信息覆盖旧条目，不新增"变更记录"小节。
- **落笔自检**：写每一行前自问，读者据此能做出什么决定或操作？答不上来就删。
- **直接写规则**：写"做什么""不做什么"，不写描述性、解释性的陈述；不为规则补举例、强调语气或说明性文字。
- "关键设计决策"只记录当前结论与必要的取舍/风险点，不归因到"业务方拍板/确认"、不写审批日期或人名；支撑结论的具体数字可大幅精简。
- 已实测的信息与推算/转述/未验证的信息必须显式区分，不能混排成同等可信度。
- 使用中文编写，代码块、表格、列表等不同元素之间需要有空行隔开，合理缩进，避免网页渲染时出现问题。
- 全角中文字符与半角英文字符之间，应有一个半角空格；全角中文字符与半角阿拉伯数字之间，有没有半角空格都可，但必须保证风格统一，不能两种风格混杂。

## 架构说明

[按数据流/调用链描述各模块职责，每个模块一条，标注文件路径链接。这份索引是给后续开发者和 AI
按需定位文件用的，写清楚"这里有什么、什么需求会用到"比写清楚"这里是怎么实现的"更重要——
后者读代码本身就能知道。每条建议包含：文件路径、核心函数/类名、一句话职责，模块较多时可以
再加一两个"涉及 xxx 时看这里"的关键词，帮助按需求关键字快速匹配到该找哪个文件，而不必逐个打开。]

数据流：`入口模块` → `取数/处理模块` → `输出/写入模块`。

- **[main.py](main.py)** —— [核心函数/类名]：[职责说明]。[可选：常见需求关键词，如"改环境变量/CLI 参数看这里"]
- **[module_a.py](module_a.py)** —— [核心函数/类名]：[职责说明]。

[体量较大的参考文档在此列出，并注明禁止一次性整份读取，需要时按关键字定位后读取局部内容。]

## 关键设计决策

[记录那些"长期有效、改动前必须理解背景"的决策——通常是踩过坑、权衡过多个方案后选定的做法。
每条给一个小标题 + 简短说明，客观陈述当前行为与必要的取舍/风险点，不写"为什么当时这么改"的叙事性语言。]

### 1. [决策标题]

[说明当前行为、涉及文件/函数，以及不这样做会有什么后果。]

## 配置与凭证约束

- **【MUST】** 密码、AK/SK、API token 等所有凭证通过环境变量或不入库的配置文件注入，**绝不入库**；新增配置项时同步更新对应的示例配置文件与 README.md。
- **【MUST】** `.gitignore` 中确认包含实际配置文件、凭证文件与运行时生成的状态文件。
- **【SHOULD】** 权限最小化：只读阶段使用只读凭证，写入所需的凭证单独保管、按需注入。

## 运行与部署

[列出本地运行、Docker 部署等命令。Docker 场景下如涉及数据持久化 / 配置文件挂载 / 时区设置等，在此列出 MUST 约束。]

```bash
[运行命令示例]
```

### 测试与检查

[列出测试、lint、类型检查等命令；没有配置测试/lint 时明确写"没有配置测试、lint 或构建流程"，
避免后续开发者/AI 反复猜测或试图运行不存在的命令。]

```bash
[测试/lint/类型检查命令示例，或注明"无"]
```

## 实现状态

[如项目按阶段推进，用"已实现 / 未实现 / 待办 / 待拍板"分类列出，避免文档与代码脱节。]

- **已实现**：...
- **未覆盖**：...

## 开发执行检查清单

### 需求确认阶段

- [ ] 已按"需求处理流程"复述需求、给出简要计划，并等到用户确认；琐碎调整除外

### 编码阶段

- [ ] 是否遵循本文件"核心设计原则"中的约束
- [ ] 网络请求 / 外部调用是否捕获异常，不中断整体流程
- [ ] 敏感信息未出现在日志、异常信息或提交内容中
- [ ] 涉及配置项的改动，示例配置文件与 README.md 是否同步更新

### 验证阶段

- [ ] 本地/测试环境运行核心流程无报错，行为符合预期
- [ ] 重复运行确认幂等，如适用
- [ ] Docker 构建与运行正常，如适用

### 提交阶段

- [ ] 功能变更时更新 README.md，设计原则或约束变更时更新本文件
- [ ] 提交信息简明扼要，只说动机/影响范围，不逐条罗列改动的函数/字段细节
